import { LitederectiveDirective } from './litederective.directive';

describe('LitederectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LitederectiveDirective();
    expect(directive).toBeTruthy();
  });
});
